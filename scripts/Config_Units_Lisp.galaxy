// Config_Units_Lisp.galaxy
// Unit configuration in s-expression format with recursive function
// Format: (define var 'value) (defun name (params) body) (funcall args)

const string gv_configUnitsLisp = 
"(define m 'Marine) " +
"(define (spawn-marines count x) " +
"  (if (> count 0) " +
"    (begin " +
"      (spawn m 1 x 0 90) " +
"      (spawn-marines (- count 1) (+ x 1))) " +
"    (log \"Spawned Marines\"))) " +
"(spawn-marines 10 3)";
